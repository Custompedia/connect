{
  "name": "custompedia-connect",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Landing 'Audit Socmed Gratis' + endpoint lead (Cloudflare Worker + Static Assets).",
  "scripts": {
    "dev": "wrangler dev",
    "test": "node --test \"test/**/*.test.mjs\"",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
